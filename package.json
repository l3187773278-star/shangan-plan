{
  "name": "shangan-plan",
  "version": "1.0.0",
  "private": true,
  "description": "上岸计划 · 通用备考版：零依赖的多考试备考助手 PWA（倒计时 / 番茄钟 / SM-2 间隔重复 / AI 出题 / 局域网双端同步）",
  "main": "core.js",
  "scripts": {
    "start": "node serve.js",
    "test": "node --test",
    "check": "node --check core.js && node --check js/bus.js && node --check js/exams.js && node --check js/ui.js && node --check js/data.js && node --check js/timer.js && node --check js/views.js && node --check js/boot.js && node --check serve.js && node --check sw.js",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "prettier": "^3.3.3"
  },
  "keywords": ["备考", "PWA", "间隔重复", "SM-2", "vanilla-js", "zero-dependency"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
